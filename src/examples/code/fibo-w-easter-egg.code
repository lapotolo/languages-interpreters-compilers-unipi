var fibo = 0 in
seq
  var n = 10 in
  var x = [0] times n in
  var i = 2 in
  seq
    x[0] := 0;
    x[1] := 1;
    while (i < n) do
      seq
        x[i] := x[i-1] + x[i-2];
        i := i+1.;
    fibo := x[i-1].;
  let the_answer = 42 in fibo.